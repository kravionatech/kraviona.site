'use client';import {useEffect,useState} from 'react';import {call} from '../../lib/api';export default function Subscribers(){const[x,setX]=useState<any[]>([]);useEffect(()=>{call('/subscribers').then(setX)},[]);function csv(){const body='email,status,subscribedAt\n'+x.map(s=>[s.email,s.status,s.subscribedAt||''].join(',')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([body]));a.download='subscribers.csv';a.click()}return <><div className="top"><h1>Subscribers</h1><button onClick={csv}>Export CSV</button></div><div className="panel">{x.map(s=><div className="row" key={s._id}><span>{s.email}</span><span className="muted">{s.status}</span></div>)}</div></>}
