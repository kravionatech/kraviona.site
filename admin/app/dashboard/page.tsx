'use client';import {useEffect,useState} from 'react';import {call} from '../../lib/api';export default function Dashboard(){const[d,setD]=useState<any>();useEffect(()=>{call('/dashboard').then(setD).catch(()=>{})},[]);return <><div className="top"><div><h1>Dashboard</h1><span className="muted">Editorial operations at a glance</span></div><a className="btn" href="/posts/create">New post</a></div><div className="grid">{[['All posts',d?.posts],['Published',d?.published],['Pending comments',d?.pendingComments],['Subscribers',d?.subscribers]].map(x=><div className="card" key={x[0]}><div className="muted">{x[0]}</div><div className="metric">{x[1]??'—'}</div></div>)}</div><div className="panel"><h2>Recent AI content</h2>{d?.recentAI?.map((p:any)=><div className="row" key={p._id}><span>{p.title}</span><span className="muted">{p.status} · {p.generatedBy}</span></div>)||<p className="muted">Loading…</p>}</div></>}
