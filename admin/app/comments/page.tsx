'use client';import {useEffect,useState} from 'react';import {call} from '../../lib/api';export default function Comments(){const[x,setX]=useState<any[]>([]);const load=()=>call('/comments?status=pending').then(setX);useEffect(()=>{load()},[]);async function mark(id:string,status:string){await call(`/comments/${id}`,{method:'PATCH',body:JSON.stringify({status})});load()}return <><div className="top"><h1>Comment moderation</h1></div><div className="panel">{x.length===0&&<p className="muted">The queue is clear.</p>}{x.map(c=><div className="row" key={c._id}><span><b>{c.user?.name||'Reader'}</b><br/>{c.content}</span><div className="tabs"><button onClick={()=>mark(c._id,'approved')}>Approve</button><button className="danger" onClick={()=>mark(c._id,'spam')}>Spam</button></div></div>)}</div></>}
