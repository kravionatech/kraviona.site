'use client';
import {useEffect,useRef} from 'react';
const tools=[['H2','formatBlock','h2'],['H3','formatBlock','h3'],['B','bold',''],['I','italic',''],['Quote','formatBlock','blockquote'],['• List','insertUnorderedList',''],['1. List','insertOrderedList',''],['Link','createLink','prompt']];
export default function RichEditor({value,onChange}:{value:string,onChange:(v:string)=>void}){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(ref.current&&ref.current.innerHTML!==value)ref.current.innerHTML=value||'<p>Start writing your story…</p>'},[value]);function run(cmd:string,arg:string){let v=arg;if(arg==='prompt')v=window.prompt('Paste the destination URL')||'';if(v||arg!=='prompt')document.execCommand(cmd,false,v);ref.current?.focus();if(ref.current)onChange(ref.current.innerHTML)}return <div className="rich-editor"><div className="editor-toolbar" role="toolbar" aria-label="Formatting tools">{tools.map(([label,cmd,arg])=><button type="button" key={label} onMouseDown={e=>{e.preventDefault();run(cmd,arg)}}>{label}</button>)}</div><div ref={ref} className="editor-canvas" contentEditable suppressContentEditableWarning onInput={e=>onChange(e.currentTarget.innerHTML)} /></div>}
