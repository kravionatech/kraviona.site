export const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:4000/api';export async function call(path:string,options:RequestInit={}){const r=await fetch(API+path,{...options,credentials:'include',headers:{'Content-Type':'application/json',...options.headers}});if(r.status===401&&location.pathname!=='/login')location.href='/login';const data=r.status===204?null:await r.json();if(!r.ok)throw new Error(data?.error||'Request failed');return data}
