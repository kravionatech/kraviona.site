interface ContainerAdProps { className?: string; }

/** Disabled because the provider script caused client-side navigation and rendering errors. */
export default function ContainerAd(_: ContainerAdProps) {
  return null;
}
