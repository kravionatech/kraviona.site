import {api} from '../lib/api'; import type {MetadataRoute} from 'next'; export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||'https://kraviona.site';try{const [p,c]=await Promise.all([api('/posts?limit=100'),api('/categories')]);return[{url:base},{url:`${base}/blog`},{url:`${base}/newsletter`},...p.items.map((x:any)=>({url:`${base}/blog/${x.slug}`,lastModified:x.updatedAt})),...c.map((x:any)=>({url:`${base}/category/${x.slug}`,lastModified:x.updatedAt}))]}catch{return[{url:base},{url:`${base}/blog`}]}}
