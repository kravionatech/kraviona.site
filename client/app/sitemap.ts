import type { MetadataRoute } from 'next';
import { api } from '../lib/api';
import { SITE_URL } from '../lib/site';
export default async function sitemap():Promise<MetadataRoute.Sitemap>{try{const[p,c]=await Promise.all([api('/posts?limit=100'),api('/categories')]);return[{url:SITE_URL,lastModified:new Date(),changeFrequency:'daily',priority:1},{url:`${SITE_URL}/blog`,lastModified:p.items[0]?.updatedAt||new Date(),changeFrequency:'daily',priority:.9},{url:`${SITE_URL}/newsletter`,changeFrequency:'monthly',priority:.6},...p.items.filter((x:any)=>!x.seo?.isNoIndex).map((x:any)=>({url:`${SITE_URL}/blog/${x.slug}`,lastModified:x.updatedAt,changeFrequency:'weekly' as const,priority:.8})),...c.map((x:any)=>({url:`${SITE_URL}/category/${x.slug}`,lastModified:x.updatedAt,changeFrequency:'weekly' as const,priority:.7}))]}catch{return[{url:SITE_URL,changeFrequency:'daily',priority:1},{url:`${SITE_URL}/blog`,changeFrequency:'daily',priority:.9}]}}
