import {API} from '../../../lib/api';export default async function Confirm({searchParams}:{searchParams:Promise<{token?:string}>}){const {token}=await searchParams;let msg='Invalid confirmation link.';if(token)try{msg=(await(await fetch(`${API}/newsletter/confirm?token=${token}`)).json()).message}catch{}return <div className="form"><h1 style={{fontSize:'3rem'}}>{msg}</h1><a className="btn" href="/blog">Browse articles</a></div>}
