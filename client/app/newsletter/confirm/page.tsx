import type {Metadata} from 'next';import {API} from '../../../lib/api';export const metadata:Metadata={title:'Confirm subscription',robots:{index:false,follow:false}};export default async function Confirm({searchParams}:{searchParams:Promise<{token?:string}>}){const {token}=await searchParams;let ok=false,msg='This confirmation link is invalid or has expired.';if(token)try{const r=await fetch(`${API}/newsletter/confirm?token=${encodeURIComponent(token)}`,{cache:'no-store'});const d=await r.json();msg=d.message||msg;ok=r.ok}catch{}return <div className="form-page"><div className="form"><div className="eyebrow">Newsletter confirmation</div><h1>{ok?'You’re in.':'Link not confirmed.'}</h1><p>{msg}</p><a className="btn" href="/blog">Explore the journal →</a></div></div>}
