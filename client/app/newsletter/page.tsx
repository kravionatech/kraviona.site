'use client';import {useState} from 'react';import {API} from '../../lib/api';export default function Newsletter(){const[msg,setMsg]=useState('');return <form className="form" onSubmit={async e=>{e.preventDefault();const email=new FormData(e.currentTarget).get('email');const r=await fetch(`${API}/newsletter/subscribe`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});setMsg((await r.json()).message||'Please try again.')}}><div className="eyebrow">One thoughtful email</div><h1 style={{fontSize:'3rem'}}>Keep the useful ideas coming.</h1><p>No noise. Just our best new guides and field notes.</p><input name="email" type="email" required placeholder="you@example.com"/><button>Subscribe free</button><p>{msg}</p></form>}
