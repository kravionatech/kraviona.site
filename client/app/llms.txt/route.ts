import {api} from '../../lib/api';import {SITE_DESCRIPTION,SITE_URL} from '../../lib/site';export async function GET(){let posts:any[]=[];try{posts=(await api('/posts?limit=50')).items}catch{}const body=`# Kraviona\n\n> ${SITE_DESCRIPTION}\n\n## Main sections\n- [Journal](${SITE_URL}/blog)\n- [Newsletter](${SITE_URL}/newsletter)\n\n## Published stories\n${posts.map(p=>`- [${p.title}](${SITE_URL}/blog/${p.slug}): ${p.quickAnswer}`).join('\n')}\n`;return new Response(body,{headers:{'Content-Type':'text/plain; charset=utf-8'}})}
